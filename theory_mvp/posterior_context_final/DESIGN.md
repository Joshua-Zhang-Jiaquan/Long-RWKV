# Fixed fresh-condition mechanism assessment

This evaluates 16 systematic and16 paired-parity base problems (indices0–31, alternating family), test seed20271001, paired across short/1K/4K/16K and all three long evidence placements. Native whole-record padding, public output labels, and the three reveal policies are identical to the completed development probe. All32 conditions and all10 contexts are retained for each checkpoint, irrespective of short competence. Numerical failures stop execution and are not omitted.

The two checkpoint selectors are fixed before this panel produces any model outputs: (1) seed71 independent2500, selected after earlier confirmation for short competence; (2) that same checkpoint after the single declared200-update context adaptation (recipe/stage dacbbe1355018e7f), regardless of development outcome. The latter checkpoint hash is bound in the evaluation receipt when training completes. No recipe or terminal-step changes are allowed under this design. The two checkpoints share a training lineage; they are not independent training replications.

Primary estimand: dependent-family, information-set improvement over one call in forward KL at16K, averaging far/middle/near equally within each base problem. Report the same-call information-set versus pair-preserving contrast, validity, all cells, and paired interactions relative to short context. Separately report adapted-minus-unadapted changes using the same paired problems. Positive KL gain means comparator KL minus information-set KL; positive validity gain means information-set validity minus comparator validity.

Use10000 paired percentile bootstrap draws, seed20271002, resampling base problems while keeping policies, positions, lengths and both checkpoints together. Intervals are pointwise95%, not multiplicity-adjusted or uncertainty over training seeds. Exact endpoint enumeration removes rollout noise, not variability among problems.16 problems/family is an MVP precision choice: development revealed large effects and each exact80-condition shard costs several minutes after kernel initialization. Report the resulting interval width, including any inconclusive result; no sample extension based on significance.

Selection disclosure: this is a fresh-condition assessment conditional on a selected training lineage. The test structure catalog was used in the earlier frozen campaign, including the competence-based checkpoint selection; it is not a new untouched structure catalog. The new seed fixes fresh public conditions within that catalog. All earlier seed failures remain reported. No broad language quality, reliable training across seeds, or architecture-causal claim follows.

Four independent8-H100 jobs per checkpoint, one base problem per rank; eight jobs total, admitted under the shared32+16 cap. The unadapted evaluation can run while adaptation finishes. This is an accuracy/mechanism panel; existing timing screens remain separate and do not automatically certify a joint quality/cost operating point on this changed task.
