# Prospective confirmation plan

Status: panel, partitions, and training/evaluation recipe frozen. Three fresh-seed parent training jobs are submitted; confirmation training and evaluation are not complete. This is not a report of confirmation outcomes.

## Fixed panel and its limits

PANEL.json has SHA256 146737f87dba9abe14a4d6cab77cb191cc2507fb132a36276405975d221f6766. It contains two fresh syndrome/structure pairs for every canonical test structure:22systematic conditions over11structures and32parity conditions over16structures. The first eligible examples in a fixed generator stream are selected, excluding all138unique condition pairs previously used by the primary pilot's sampling and calibration panels. Exact-audit and runtime-probe conditions are subsets of those excluded panels. No model outputs inform selection.

All27structures remain disjoint from training and development catalogs. The panel reuses the existing held-out structure catalog; it does not claim new graph topologies. Its condition distribution is explicitly stratified over structures and restricted to previously unevaluated syndrome pairs. Surface names and equation order retain the existing generator's distribution. The panel is not an IID sample of independent unseen tasks, nor does it measure natural-language quality.

The generator and exclusion tests are lrwkv_evidence/train04/confirmation_panel.py and tests/test_confirmation_panel.py. Panel construction passed its coverage, uniqueness and exclusion checks. Keep the panel immutable. Do not run a model on it during recipe selection.

## Development decisions before confirmation

Complete and report the four development arms: hard/soft targets crossed with ordinary corruption/policy-history mixture. The latter branch starts from the corresponding completed300-update parent model and optimizer; it is a controlled intervention from that point, not a new from-scratch initialization.

Review commonN4development and exact endpoint audits at step700. Continue the originalN8terminal task through the declared1500-update endpoint, or explicitly document a new development protocol if evidence requires a change. The two-bit result is never a substitute forN8. Use common development endpointKL and calibration, both families, to assess competence. The previously stated0.1nat information-setKL is a useful quality target, not an achieved result or a reason to suppress failures.

Before any confirmation output is read, record a frozen recipe manifest: source hashes, serialized input format, all curriculum transitions, label and mask objectives, optimizer and initialization settings, terminal checkpoint rule, precision settings, chosen treatment and its matched control. Report all development arms, including unsuccessful ones. Do not select an interim test checkpoint.

For a claimed policy-mixture training benefit, the matched control must use the same label objective and ordinary corruption. For a claimed soft-target benefit without a mixture intervention, the control must use hard targets under the same mask law. If neither intervention helps onN8, report that failure rather than calling the N2 result a terminal-task success.

## Replication and evaluation target

The intended replication target is three fresh data/corruption seeds53,71,89 for the selected recipe and its matched training control, starting from the same released model and declared initialization seed. These are data-order/corruption replicates, not independent pretrained initializations. Any change to this target must be recorded before confirmation evaluation. The existingseed17development checkpoints are reported separately.

For every fixed terminal checkpoint, evaluate one-round, information-set two-round, matched random-halves two-round, and sequential eight-round sampling on the fixedN8panel. Freeze the condition-indexed random partitions and share them across checkpoints. These input-derived groups are now frozen in PARTITIONS.json (SHA256 4843768ddcf506d901dcf862ca5c3fd4ea770dececa78ae40efdd983cf64ab93), generated by lrwkv_evidence/train04/confirmation_partitions.py without model access. The training recipe was subsequently frozen as recorded below. Information-set selection uses publicA only and never supplies solved output values to the neural sampler. Two-round methods must each make exactly two calls and reveal four original coordinates per call. Endpoint enumeration caches are audit optimizations, not decoding-speed measurements.

Primary scheduling comparison: random-halves endpointKL minus information-set endpointKL on the parity family, reported per training seed, per structure, and as an equal-structure mean. Also report absolute endpointKL, analytic dependenceD, independently accumulated estimationE, exact valid mass, and conditional-within-valid-supportKL. Systematic constraints are a separate zero-dependence control. Do not average away an adverse family or hide a seed with the wrong contrast sign. Equal-family summaries are explicitly50/50, despite unequal family counts.

Numerical qualification precedes confirmation. The local kernel probe supports explicit TRITON_F32_DEFAULT=ieee for accurate recurrence arithmetic, but current development training used the common default Triton configuration. Qualify explicit-IEEE full-model development inference, repeated canvases and wrapper equivalence before fixing confirmation inference. Do not change precision after observing confirmation outcomes. Full-model official-RWKV parity is a separate unproven claim.

Exact enumeration removes endpoint Monte Carlo error for these fixed conditions; it does not remove uncertainty about training, rendering, or unseen conditions. Report three-seed values and ranges. Avoid broad population-significance claims from three fitted models. Any additional sampling panel must have a fixed draw count and coupled randomness declared in the frozen recipe manifest.

## Paper claim boundary

A positive result can support a controlled mechanism in a restricted binary posterior with known public constraints and oracle soft supervision where used. It does not establish a learned dependency selector, memory compression, a new general KL identity, natural-language quality, long-context superiority, or an architecture advantage. Existing frozenF2and failed500-update results remain separate and unchanged. Completion requires the final paper's claims to match the executed experiments and proof assumptions, regardless of whether the result is positive.

## Frozen selection and execution

FROZEN_RECIPE.json has SHA256
8cd809d08a96ea1372bc9b8404167a49bf43bac55a8beddc909550dd574120ba.
The completed terminal development decision is
results/train04paired/phase1000/DECISION.md. Both paired-history arms failed to
learn eight-bit parity dependence; complementary histories improved calibration.
The selected comparison tests coupling under the same population objective,
not policy-mixture versus ordinary corruption or soft versus hard supervision.
The target remains three complete fresh-seed replicates with a matched control.

Parent training for seeds53,71,89 is submitted in immutable stage
8cd809d08a96ea13,8H100each. Every parent starts from the released checkpoint and
trains the full1500-update curriculum before its two1000-update branches.
The20-update runner qualification passed and is discarded. No confirmation
model has been evaluated. All sources, initialization/optimizer/curriculum
settings, checkpoint rules, panel/partition hashes, and IEEE inference settings
are frozen in the manifest before any confirmation output is read.

## Authorized resource update, 2026-09-22

After all three parents completed, the user authorized an additional 16 H100s
from project-632c8db8-4530-413a-ada5-df91774a7e09. The allocation is now
32 H100s on the original project and 16 on that project, with both queued and
running jobs counted. Six eight-GPU branches can therefore overlap. The frozen
recipe, data, optimizer, seeds, checkpoint rules, and inference precision remain
unchanged. Resource authorization and scheduler-spec evidence are recorded in
results/train04confirm/capacity_authorization.json and
results/train04confirm/extra_project_resource_specs.json.
