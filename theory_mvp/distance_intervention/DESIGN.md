# One-day matched distance and evidence intervention

This prospective follow-up retains all preceding negative results. It tests whether long-distance training reduces conditional error enough to realize the theoretical dependence reduction of a second diffusion pass. The existing 16K two-call quality claim fails. No positive outcome is assumed.

## Training intervention

Start every arm from the selected seed71 independent2500 checkpoint (SHA52dbc7f289513e475d10422dbccffa52f74ec55fc887253ea13ae0476dc3e2c9), with fresh optimizer. Compare near-only public-block placement against balanced far/middle/near placement. All inputs have 16K native-token prefixes, with the same complete distractor records, original public block, and labelled output slots. Instructions move with equations; this is a public-block distance intervention, not an isolated factual-memory manipulation. Both arms use identical examples, targets, two independently drawn target histories, policies, rounds, seeds and updates. The order of prefix tokens is the sole input difference.

Use adaptation seeds20271011,20271012,20271013, with both arms retained for every seed. These are adaptation replicates of a selected lineage, not independent pretraining or from-scratch learning replicates. Global batch32, eight ranks, four serial microbatches/rank with gradient accumulation. Train both information-set and intact-pair two-call policies equally, each with a uniformly sampled round and exact conditional soft cross entropy. Half public examples are systematic, half paired parity. AdamW lr1e-5, warmup20, betas(.9,.95), weight decay.01, clip1, FP32/IEEE, TF32off. Training indices start2,000,000 and use train structures only.

Run one discarded six-update qualification from the original checkpoint. Check document isolation at all three positions, all finite gradients, measured time and memory. Qualification checkpoints are never evaluated as final models. After qualification, freeze a common terminal update count based ONLY on runtime: min(600, floor(36000 / max(seconds for qualification updates4..6) / 12)*12). This reserves ten steady-state training hours, with a 13-hour scheduler limit per main job. If fewer than24updates fit, record infeasibility and revise the operational plan before submitting main jobs; do not silently shrink the scientific design. No early stopping, quality-based selection, or automatic job retries. Every main job starts afresh. Max48 queued+running H100s, primary32 extra16, max8/job. Fastest completion takes precedence over artificial utilization targets; record actual utilization and memory.

## Frozen evaluation specification

All six fixed terminal checkpoints are evaluated regardless of training loss. Fresh test seed20271021, paired-parity base indices0..31. Primary panel32 base problems x three positions at16K, with all three policies: one, information_set, pair_preserving_halves. Exact enumeration of all16 valid outputs, normalized model conditionals, KL=D+E decomposition, support mass and within-support distortion. Resample base problems jointly across arms, positions and seeds for10000 paired bootstrap draws, seed20271022; report all three seed effects and the seed-averaged effect, with intervals explicitly conditional on these adaptation seeds and selected lineage. Do not interpret problem-resampling intervals as population training-seed uncertainty. Test structures come from the previously observed held-out catalog; fresh prompts do not establish untouched structures.

Primary causal contrast: near-arm minus balanced-arm information-set conditional error at16K far. Additional contrasts: matched position interaction, joint-KL gain overone, and same-call intact-pair minus information-set KL decomposed into D and E. Dependence-only prediction is4ln2; equality is expected only if conditional errors are equal or negligible. A successful controlled mechanism result requires positive far-error improvement, directionally consistent across adaptation seeds, plus the evidence-response intervention below. A positive16K quality-cost result additionally requires information-set KL below4ln2 and below the same model's one-call KL, with measured mean latency within1.5s for the unchanged restricted policy set. Existing attention costs support only the previously stated fixed implementation/budget comparison.

For each primary base problem, flip the syndrome of one publicly selected pair (row index=index mod4). Keep public labels, constraint order, distractors and token positions fixed. Hold all four information-set visible values at zero for this diagnostic; do not draw a model history. The flip changes exactly one target coordinate in the remaining group. Evaluate both evidence variants and report the signed target logit contrast, probability shift, average correct conditional CE, and absolute change on unaffected remaining coordinates. Verify the native prefixes differ only at the intended syndrome token and have equal length. This fixed-history counterfactual is a local response diagnostic, not the full target-history average E. The normal exact panel separately integrates all histories.

Secondary panels: indices0..7 at evidence-only and4K far/middle/near; indices0..7 at32K far and near are exploratory untrained-length stress tests. Also evaluate the original starting checkpoint on the same panel as a descriptive baseline, without adding it as an independent seed. No outcome-dependent omissions. Precision/timing rechecks use the same inference interface and hardware.

Fresh-condition generation and GPU evaluation code will be hashed in a separate evaluation manifest before any final-checkpoint predictions are inspected. CPU tests must verify counterfactual oracle changes, matched token layouts, matched training examples, correct supervision for both schedules, and exact oracle endpoints. No final examples enter training or optimizer choices. All negative seeds/positions remain in reporting.

## Deliverables and stop rule

Immutable source/job/checkpoint receipts, per-step token/time/memory accounting, utilization traces, exact raw endpoint and counterfactual outputs, paired analysis, mechanism figure, and an updated paper with supported claims. The campaign has a one-day target including evaluation; runtime/queue delays are reported rather than silently selecting early checkpoints. Report failures honestly and retain the existing frozen experiments. No broad benchmark expansion, architecture change or fresh0.4B pretraining is included.
