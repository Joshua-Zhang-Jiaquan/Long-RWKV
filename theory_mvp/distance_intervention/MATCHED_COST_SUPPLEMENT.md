# Prospective matched cost supplement

Frozen before any of the six adapted terminal models produces evaluation outputs. The original-checkpoint evaluation has completed; all adaptation recipes and analysis selectors remain fixed.

Reason: the preceding attention cost panel uses different public conditions. Re-measure the same qualified Pythia410M bidirectional model on exactly the eight fresh paired-parity problems (indices0..7, seed20271021), three16K positions and three fixed policies already used by the new RWKV timing protocol. One warmup plus five complete synchronized batch-one requests per cell;360 measured requests total. FP32/IEEE, torch TF32 disabled, eight independentH100 ranks. Source/checkpoint hashes, native prefix lengths and numerical repeat/cross-rank checks are retained. Native tokenizers differ and therefore equal native lengths can include different distractor-record counts. No attention quality claim or additional attention training is introduced.

The budget remains1.5s mean request latency, the threshold fixed in the preceding campaign and preserved in the current frozen evaluation manifest. Report all costs regardless of quality outcomes. For each of the six fixed adapted checkpoints, test the sufficient quality-cost certificate on this SAME eight-problem empirical context law, averaging its three positions equally. Quality is taken from the already frozen exact endpoint panel, with no new checkpoint selection. Require recurrent information-set mean KL<4ln2, recurrent two-call mean latency<=1.5s, attention one-call mean latency<=1.5s, and both attention two-call mean latencies>1.5s. Report paired-problem pointwise95% intervals separately; a plug-in empirical certificate is not a population guarantee or per-request deadline. Report all six checkpoint certificates, not just any successful checkpoint. The32-problem primary distance-intervention analysis remains unchanged. No result on eight problems is relabelled as that primary32-problem result.

Admission uses the existing32+16H100 capacity check and no more than eight GPUs. The run waits for training/evaluation capacity to free and does not stop or modify an admitted frozen job. No retries without diagnosis. Final delivery includes this measured comparison even if every quality certificate fails.
