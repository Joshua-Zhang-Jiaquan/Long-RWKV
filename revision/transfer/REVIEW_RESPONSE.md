# Working author-response position

The review identifies a significance gap that the current evidence audit does not resolve. The main numbers are reproducible, but the demonstrated position intervention and near-oracle policy contrast do not establish a transferable predictive method.

The proposed revision targets a new, falsifiable result: source-calibrated selection between equal-cost, zero-factorization-error policies on held-out dependency classes. The original error decomposition will be credited as established accounting. Six fresh full training lineages are running under the frozen protocol, with all failures retained. Their outcomes will test whether the result extends beyond selected seed71.

The existing restricted cost certificate remains valid within its declared comparison class. It will not be presented as an optimized practical frontier. A matched competent causal baseline and qualified attention optimization are required if that practical contribution is pursued instead.

The selected initial model and all six near/balanced adaptations are now available as [lossless inference exports](https://github.com/Joshua-Zhang-Jiaquan/Long-RWKV/releases/tag/original-inference-v1). All 872 model tensors and each checkpoint contract were verified equal to their originals. Removing optimizer state reduces each file to about 1.7 GiB; the registry records both original and export container hashes. Anonymous downloads are accessible. The export wrapper validates these identities and binds copied evaluation manifests to the new containers without changing scientific code or task panels. This resolves the original inference-checkpoint access limitation; it does not constitute an independent neural replication.

The `tools/reconstruct_lineage.py` workflow also starts from the pinned public base, includes parent1500 plus the independent branch through2500, all six adaptations, and evaluations under new checkpoint identities. Its portable launch/source validation has passed; independent retraining of the older task has not been rerun. The new study separately provides exact training/evaluation capsules and a complete portable command workflow.

No new neural transfer, independent-lineage success, or optimized-baseline superiority is claimed in this response. `FROZEN.json` now fixes the additional experiment; the CPU feasibility results establish only that the proposed equal-D controlled comparison and genuinely different code classes exist.
