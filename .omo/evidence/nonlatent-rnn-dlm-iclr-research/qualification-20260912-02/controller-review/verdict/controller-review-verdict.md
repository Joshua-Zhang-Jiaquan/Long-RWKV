# Qualification 02 renewed-controller review — PASS

Date: 2026-09-13

Decision: **PASS the focused controller/request review. The renewed permit is suitable for one fresh live CreateJob call only after root changes exactly its four review gates to `PASS`, `CLEARED_NO_MATCH`, `PASS`, and `READY_FOR_SINGLE_SUBMISSION`, preserving every immutable field and the exact request bytes. No live action was performed or authorized by this review itself.**

## Findings

1. **Actual authority is the renewed campaign.** `.omo/authorizations/nonlatent-gpu-campaign-20260912.json` records the explicit instruction `unlimited usage for gpu and go on`, no user-imposed cumulative GPU-hour ceiling, authorization for a replacement qualification, preservation of the prior attempt, and finite per-job controls. `CampaignAuthorization` validates that file and its current SHA-256 `067e063296ab8a738ec796afb3e92aa2732a0da261360285a591c84c05221b0a` against both permit and controller state.
2. **Legacy profile 01 is compatibility metadata, not renewed authority.** Permit and state bind campaign authorization, unique permit `permit-nonlatent-gpu-campaign-20260912-e42a9149-a2da-429a-b0c5-a8315c07f7d2`, and fresh grant instance `grant-qualification-20260912-02-2e9052ad-a45a-4e6d-868b-1dc2b3415039`. The old authorization ID is used only because the already-approved payload receipt schema/run-ID prefix is immutable. Admission evidence retains campaign permit/grant identity, so the controller does not represent the old grant as unused.
3. **The old attempt remains consumed-or-unknown.** The immutable attempt marker still records one CreateJob call at `2026-09-12T08:30:43Z`; its sanitized receipt says process exit zero, no captured valid ID, admission `UNKNOWN`, and no retry. Original controller source hashes still equal the previously reviewed values. Fresh filtered old-UUID/prefix queries observed no match at `2026-09-13T02:49:19Z` and `02:49:26Z`, but correctly do not claim the old backend request never arrived.
4. **Fresh identity is distinct.** Request/run name is `qualification-20260912-01-28575096-6ce0-4fd8-9e86-3e3804c86c14`, retained under the legacy prefix solely for payload schema compatibility; its UUID, nonce `0f5967954a5daa239f738de73b7c4649`, permit ID, grant instance, qualification-02 state path, and GPFS receipt destination differ from attempt 01.
5. **Exact fresh request passes.** `exact_job_spec.json` is 1,694 bytes with SHA-256 `ece8b1bf041f31ee84bb9b400d21856c31c458770982b75568b7be31c6f0c42e`. It requests one instance of the resolved eight-GPU H100 SXM 80 GB pool/spec, exactly `1800000` ms/four GPU-hours, fault tolerance false, retry zero, both reserve durations zero, unchanged project/workspace/LCG/image/manifest, launcher-only command, and exact run/nonce/manifest/receipt environment.
6. **Response capture precedes admission parsing.** The controller exclusively writes/fsyncs the permit/grant-bound attempt marker, makes one 120-second CreateJob invocation, then records return code or timeout/invocation status; raw stdout/stderr SHA-256 and byte lengths; and redacted actual stdout/stderr in mode `0600`. Only after those writes complete does it import and call the admission classifier. Partial timeout output and invocation failures follow the same capture boundary.
7. **Sanitization controls pass.** Persisted response content is redacted recursively for sensitive JSON keys and for Bearer/text token, password, credential, auth, key, cookie, and secret assignments. Raw response bytes are not persisted; only hashes and lengths are. CLI errors expose only typed controller codes, or the exact admitted job ID after admission. CPU tests cover JSON/text secrets and timeout partial stderr; the existing dry-run capture is mode `0600` with matching content hashes/lengths and no credential content.
8. **Success and failure classification is fail-closed.** A process-zero structured business error is `Rejected`; empty, non-JSON, unrecognized, wrong-action, nonzero-without-business-error, timeout, invocation failure, missing/invalid/distinct IDs are `Unknown`. Success requires one unique valid `job-UUID` from only `job_id` or `Result.job_id`; the operator `qualification-...` name cannot satisfy this type. Rejected/unknown decisions are persisted and stop without automatic retry.
9. **Actual ID is confirmed before receipt publication.** The parsed ID is exclusively persisted in campaign/permit/grant-bound `admitted-job.json`, then one captured GetJob request for exactly that ID must return the same ID and reserved name plus exact project/workspace/LCG/runtime/fault/retry/priority/image/spec/one-instance/eight-H100 binding. Only `ConfirmedJob` produces the strict payload `ControllerReceipt`, validated by the unchanged payload model and published through same-directory temporary + atomic no-clobber link. Any post-admission failure reports that exact ID and never creates another job.
10. **One-call/reconciliation behavior passes.** The attempt marker is written before the runner and is exclusive. Existing attempt without admission yields `submission_attempt_requires_reconciliation`; existing attempt with a validated admission can only re-confirm the known ID with GetJob. There is no path from an ambiguous CreateJob response to another CreateJob call under this scoped permit.
11. **Root may flip only the explicit review gates.** An all-temporary probe changed only `controller_review_verdict`, `prior_ambiguous_reconciliation_verdict`, `live_submission_verdict`, and `status`; the real context loader and live gate then accepted while request bytes and grant identity remained unchanged. A per-job runtime change was rejected by literal validation. The current permit hash `06e6f0c2...` and artifact receipt are pre-review snapshots and will intentionally become historical after root's four-field edit; state binds campaign and request hashes, not a frozen permit hash.
12. **Focused verification passes.** All 26 designated tests passed in 0.35 seconds with `PATH=/nonexistent`, so no qz executable was available and runner behavior was fake/pure. All 33 prepared artifacts and nine input bindings verified, and LSP returned zero diagnostics for all 16 reviewed Python files. The unchanged 143-test/35-file payload review was reused by hash and not rerun.

## Remaining real unknowns

- Current permit gates are still pending/NO-GO, and fresh request-attempt, admission, CreateJob capture, initial GetJob capture, and GPFS receipt files are absent. Root must perform the four-field permit transition before any live call.
- Live quota/capacity, CreateJob response, actual job ID, initial GetJob response shape/status, mutable-tag image bytes/digest, physical GPU identity, and runtime result remain unknown until execution. `observed_image_digest` remains honestly null.
- The 02:49 reconciliation is a point-in-time filtered-index observation, not proof that attempt 01 never reached the backend. The renewed campaign authorizes replacement, but any future ambiguity must still stop for reconciliation rather than trigger a blind loop.
