# Task 03 independent CPU-subset verification

- Reviewer: `gpt-5.6-sol`
- Scope: task 3 only; read-only against product/plan/shared outputs.
- Writes: this unique evidence directory only.
- Prohibited operations honored: no qz, CUDA, network, install, git, or unpickling.
- Sandbox classification: marker-only fixture used to execute `prepare`; real repository used read-only for `verify`, `analyze`, source inspection, and hashes.

Command outputs, exit codes, proof classifications, cleanup state, and the final verdict are recorded in `report.md`.
