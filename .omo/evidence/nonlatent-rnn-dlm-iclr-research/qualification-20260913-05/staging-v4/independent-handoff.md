# V4 owned-origin fix handoff

The new immutable release is `/inspire/hdd/global_user/zhangjiaquan-253108540222/nonlatent_iclr_qualification/payloads/qualification-20260913-05-global-ffaa464d-origin-v4`. Its launcher is `/inspire/hdd/global_user/zhangjiaquan-253108540222/nonlatent_iclr_qualification/payloads/qualification-20260913-05-global-ffaa464d-origin-v4/scale/experiments/nonlatent_iclr/qualification/run_qualification.sh`, SHA-256 `3ebe224805a78606b766ce4122664a6bcf5ba33505872d44be534ace5273ba3a`. Its owned model root is `/inspire/hdd/global_user/zhangjiaquan-253108540222/nonlatent_iclr_qualification/payloads/qualification-20260913-05-global-ffaa464d-origin-v4/model_source/scale`.

The adjacent 109-entry manifest has SHA-256 `9204381bd606157440c8113556798ab4d271edd4210a44249a67139253986004`. The v4 source inventory is manifest-bound as a payload, has SHA-256 `accee7d02191adbe105cdc823218a188615be9ea1be93bbf047bf2d4ed26f23d`, and maps all 72 unchanged local model source blobs: 64 runtime-reachable and eight inactive package members. It reports no unavailable local dependency and no resolved local dependency outside `models`.

The focused origin fix sets the launcher's working directory to the owned release root and exports `PYTHONSAFEPATH=1` plus a fixed `release_root:model_source_root` `PYTHONPATH` before direct Python and torchrun children. At the model-construction boundary, the resolved staged root is always promoted to `sys.path[0]`. Any foreign preloaded `models` or `models.*` module fails before constructor access without deletion or replacement. After entrypoint import, every loaded local model module must resolve beneath the owned root and to a manifest `staged_source` file. Other package namespaces are not inspected or rewritten.

Failing-first controls selected frozen v3 and produced six origin failures with five unrelated passes in 1.55 seconds. The unchanged controls selected v4 and passed all 11 tests in 1.59 seconds. Manual QA from the exact qz-stage CWD observed v4 at `sys.path[0]`, `models/__init__.py` in the v4 owned package, and `models/birwkv7_diffusion.py` in the v4 owned package. Exact global launcher and CLI help remained available from that CWD.

The bounded environment diagnostic from the hostile CWD returned `MATCH`, loaded no runtime model modules, and matched eight package-source files plus six exact package versions. Strict CPU preflight returned `PREFLIGHT_VERIFIED` for all 109 entries. The final prior scope plus new controls passed 189 tests in 23.81 seconds. Changed-file/package-context LSP diagnostics were clean, the Python no-excuse audit reported no violations in five files, shell syntax passed, and the release is recursively read-only.

All model weights, neutral checkpoint metadata, 72 model-source blob identities, package-source pins, and architecture/runtime behavior remain unchanged. The runtime remains eight ranks, FT 0, 1,680 seconds including kill grace below the 1,800-second scheduler cap, and `PARTIAL_QUALIFICATION` with full-model cache unsupported. The expected image tag remains `docker.sii.shaipower.online/inspire-studio/relay2:v2`; its digest is unknown.

No GPU, model unpickle/construction, qz/API/network/install/git, controller, authorization, plan, Boulder, or unrelated Task-4 action occurred. Source-origin CPU proof is not GPU proof. The next step is only the requested focused independent recheck of this origin fix.
