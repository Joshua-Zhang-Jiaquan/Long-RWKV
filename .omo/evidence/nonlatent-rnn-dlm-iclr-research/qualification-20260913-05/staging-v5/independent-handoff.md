# V5 bytecode-isolation fix handoff

The immutable candidate is `/inspire/hdd/global_user/zhangjiaquan-253108540222/nonlatent_iclr_qualification/payloads/qualification-20260913-05-global-ffaa464d-bytecode-v5`. Its launcher SHA-256 is `112ab2595a9db913a40c3976a96998ccf54f9fb7a9680da1ed841cf871dbd52b`; its bytecode guard SHA-256 is `b5bcff58019e09a2acee0bf0ea6b4839371f91c3e25c32982d56f47f5c3b8a09`. The deterministic whole-tree SHA-256 after freezing and final tests is `f2972779734df10aad2213c4f96f50c78c3b3e81f49a2e95106cff380d72cc0e`.

The 110-entry manifest has SHA-256 `9271a2036e84c3fa29442a9f7627c9889d024106b8286321d82da26df856e51d`: 24 payloads, 72 staged sources, eight package sources, one HF config, one HF index, two HF shards, one checkpoint metadata file, and one checkpoint model. The 72-record inventory has SHA-256 `43dc97ccd4f80cc1e5e0cf9b698b6e0db1cd1a7f9699340242ec06851b040644`; all original/owned hashes and sizes match, and all v4/v5 source identities match after ignoring the owned path rebind.

The v4 reviewer found 16 unmanifested timestamp `.pyc` files opened and unmarshalled even with `PYTHONDONTWRITEBYTECODE=1`. V5 was copied through a bytecode/cache exclusion filter. Before the first inner-workflow Python child, the launcher now creates a fresh helper-owned `/tmp/nonlatent-qualification-pycache.*`, exports both `PYTHONPYCACHEPREFIX` and `PYTHONDONTWRITEBYTECODE=1`, and rejects `__pycache__`, `.pyc`, or `.pyo` under the owned payload and model roots. Cleanup is installed before Python and passed normal, failure, and timeout tests.

The dynamic isolated-import control observed the exact owned CWD and `release:model_source` `PYTHONPATH`, safe-path mode, no opened or unmarshalled release bytecode, an empty external cache prefix, and removal of that prefix on exit. Strict CPU preflight returned `PREFLIGHT_VERIFIED` for all 110 entries. The bounded runtime-environment check returned `MATCH`, with eight package-source and six package-version matches and no runtime model modules loaded. The retained first invocation with an empty receipt path is explicitly an `INVALID_ARGUMENT` attempt, not success evidence.

The final release-matched regression ran the historical v4 scope against v4 (`189 passed in 23.90s`) and both new suites against v5 (`11 passed in 1.47s`). A prior attempt to combine all 200 tests under one process-wide v5 selector produced `199 passed, 1 failed`; the sole failure was the intentionally v4-specific freshness test receiving v5's 110-entry manifest instead of v4's 109-entry manifest. No product code changed for that harness correction.

The v4 full-tree digest was `da594027f4ac777101d9b288a84a9f042d09ab14fbb7dfd68c0fc3d88c664c5d` both before the v5 copy and after final tests. V3 and v4 truthfully retain 16 `.pyc` files plus four cache directories each; they are forensic predecessor state, not source-clean claims. V1–v4 manifest hashes were reverified. V1–v3 do not have a newly captured same-algorithm pre-repair whole-tree snapshot, so this handoff does not invent a whole-tree before/after claim for them.

Model construction code, all 72 model source blobs, package-source pins, HF files, checkpoint identities, eight-rank topology, FT 0, 1,650-second workflow plus 30-second grace, 1,800-second scheduler cap, and `PARTIAL_QUALIFICATION` remain unchanged. No GPU, CUDA, model unpickle/construction, qz/API/network/install/Git, controller, authorization, plan, Boulder, or unrelated Task-4 action occurred. The only next step is focused independent review of the bytecode-isolation fix; no live or GPU success is claimed.
