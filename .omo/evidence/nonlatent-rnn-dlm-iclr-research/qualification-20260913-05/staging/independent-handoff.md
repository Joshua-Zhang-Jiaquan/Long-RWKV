# Worker-visible qualification staging handoff

The reviewed qualification runtime is staged at `/inspire/hdd/global_user/zhangjiaquan-253108540222/nonlatent_iclr_qualification/payloads/qualification-20260913-05-global-92fbf9af`. The exact launcher is `scale/experiments/nonlatent_iclr/qualification/run_qualification.sh` below that root. Its rebased 40-file manifest is adjacent and has SHA-256 `49cdef9722351a951ed7f36a679708905df30a19801e7d2eaa7493144638869f`.

The release contains the exact eager parent import closure rather than an empty package initializer. Twenty Python/package files remain byte-identical to reviewed project sources. The only changed runtime blob is the staged shell launcher: it now reserves the unique global output child after validating the run ID and publishes `early-failure.json` for startup failures before Python preflight. The eight-rank runtime, safe checkpoint loader, model checks, package checks, output semantics, zero-restart policy, and 1,650+30-second deadline are unchanged.

CPU staging checks pass: the prior 143-test baseline passed, all eight artifact-specific regressions pass, projectless global-only CLI validation returns structured evidence, stale payload hashes fail closed, shell syntax/help pass, and the changed test has clean LSP diagnostics. The release hierarchy is read-only and contains no tests, cache directories, HF weights, or checkpoint weights.

The release is not ready for a live request because the preserved checkpoint root `/inspire/hdd/global_user/zhangjiaquan-253108540222/outputs_birwkv_diffusion/m4-loop-2p9b/step_00004750` is now absent. Manifest preflight therefore fails concretely with `manifest_file_unavailable:meta.json`; constrained lookup also found no step-4750 `model.pt`. No path or digest was substituted. Restore the exact reviewed bytes at that path, or independently review a new checkpoint binding and manifest, before updating fresh request constants. The proper `flash-linear-attention` distribution check was not reached and must remain a next-worker preflight check rather than an inference from the earlier incorrect `fla` query.

No qz/controller job, CUDA operation, network call, installation, git operation, unpickle, model construction, or weight copy occurred.
