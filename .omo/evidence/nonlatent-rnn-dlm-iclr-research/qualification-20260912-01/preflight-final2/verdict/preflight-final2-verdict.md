# Independent payload-final2 verdict — PASS CPU PREFLIGHT

Date: 2026-09-12

Decision: **PASS CPU preflight; payload-final2 is ready for the controller/reservation gates and the sole authorized initial qualification. This is not a GPU or runtime pass.** No remaining deterministic must-fix was found in the bounded recheck of the previous NO-GO findings.

## Findings

1. **PASS — checkpoint boundary fixed.** The loader now requires a flat `collections.abc.Mapping` with string keys and `torch.Tensor` values, calls only `torch.load(..., weights_only=True, map_location="cpu", mmap=True)`, then performs `load_state_dict(..., strict=True)`. The real checkpoint remains statically identified as `collections.OrderedDict`, and the independent OrderedDict regression passes.
2. **PASS — exact authorized hardware boundary fixed.** Runtime accepts only `NVIDIA H100 80GB HBM3` and `NVIDIA H100-SXM5-80GB` at 79,000–83,000 MiB, requires eight visible devices and eight unique UUIDs, and rejects H100 NVL, H100 PCIe, and out-of-range capacities.
3. **PASS — the entire workflow is bounded.** The outer limit is 1,650 seconds with 30-second KILL grace, so the launcher ceiling is 1,680 seconds under the scheduler's 1,800-second cap. Phase limits are preflight 300, runtime 1,200, and aggregation 90 seconds (1,590 total), leaving 60 seconds inside the outer boundary for transitions/publication. Controller receipt wait is at most 60 seconds and NCCL/process-group timeout is 120 seconds, both inside runtime.
4. **PASS — controller receipt is strict and fail-closed.** Before importing torch/FLA, every rank waits for a regular, non-symlink, at-most-65,536-byte receipt. Schema literals bind the authorization ID, project/workspace/LCG/spec, requested H100 shape, one node/eight GPUs, 1,800 seconds/four GPU-hours, one submission, zero retries, requested image/catalog ID, plus dynamic job ID, exact-request hash, run ID, nonce, and manifest hash. Missing, malformed, oversized, stale run/nonce/manifest, extra-field, changed-limit, and native-job-ID mismatch cases fail.
5. **PASS — real job identity is no longer invented from runtime naming.** The validated controller receipt is the identity root. Every nonempty `QZ_JOB_ID` or `JOB_ID` must equal its qz job ID; if neither is present, origin remains explicitly `controller_qz_createjob_receipt`.
6. **PASS — image identity is honest.** Requested tag `docker.sii.shaipower.online/inspire-studio/relay2:v2` and scheduler catalog ID `image-7330d118-df9d-4e2e-82b1-c2543e831eb4` are literal bindings. `observed_image_digest` is strictly null, `QUALIFICATION_IMAGE_DIGEST` is forbidden by the launch contract and absent from production source, and no operator string is presented as pod proof.
7. **PASS — all-rank terminal binding remains fail-closed.** Rank evidence binds nonce, manifest, canonical controller receipt, exact required checks, hardware/model identities, and package/checkpoint evidence. Aggregation requires exactly ranks 0–7, rejects extra/hidden/missing/stale/malformed/non-finite/failed/inconsistent records, and requires all successful ranks to share the controller/resource/image binding.
8. **PASS — model/cache scope is correct.** Each rank must strictly load the checkpoint, prove exact geometry and parameter tensor/numel totals, run standalone forward-attention prefix/suffix cache equivalence, run an uncached custom masked forward with exact finite logits, and stay below the memory ceiling. Success is only `PARTIAL_QUALIFICATION` / `corevalid/fullmodelcacheunqualified`; FFN token-shift and tied-loop cached-prefix support remain explicitly `NOT_QUALIFIED`.
9. **PASS — current source binding.** Runtime-manifest SHA-256 independently recomputed as `92fbf9afe7d0c7b47829337639dae54debddc8e9409c0903f30358aa75aa656c`; streaming validation passed all 35 files and six package versions. The 25-entry payload hash receipt and 14-entry payload-final2 evidence receipt also match current bytes.
10. **PASS — no checkpoint unpickle or CUDA was used by this review.** Manifest streaming reverified checkpoint SHA-256 `ffaa464dabb3291c40749bbac4d6805e8a47082e3e2b082e0240365ffc525c07` and size 16,367,167,378. Static pickle inspection found the expected OrderedDict representation and no unsafe globals; `torch.load` was not called and CUDA remained uninitialized. Raw HF safetensors headers report 1,059 BF16 tensors and 2,947,735,040 elements.
11. **PASS — independent CPU execution.** The complete `scale/tests/nonlatent_iclr` suite passed 143/143 in 17.77 seconds. Targeted prior-finding tests passed 4/4; controller/result binding tests passed 16/16. Shell syntax, exact launcher/CLI/probe help, and runtime/NCCL/controller over-limit bad inputs passed with expected exits, no output artifacts, and no child processes.

## Runtime uncertainties and remaining external gates

- No future `CreateJob` response or controller receipt exists yet. The controller must persist the exact request bytes, compute their SHA-256, call CreateJob once, extract the real returned job ID only on success, and atomically publish the no-clobber receipt. This provenance is necessarily future controller evidence, not a CPU-payload claim.
- The mutable image tag's full package contents, actual H100 inventory, FLA kernels, checkpoint load, model numel, cache result, masked forward, and memory use remain unknown until runtime. Worker preflight will fail before CUDA if any of the 35 source/package/checkpoint bindings differ.
- Live resource re-resolution and the persisted unique request/run reservation remain submission-controller prerequisites handled outside this bounded recheck. Authorization remains exactly one 8×H100-SXM-80GB node, at most 1,800,000 ms / four GPU-hours, one submission, and no automatic retry.
