# Verdict: NO-GO — do not spend the sole authorized job yet

1. **P0 — module is a no-op:** `probe.py` has no `if __name__ == "__main__": raise SystemExit(main())`; the exact `python -m ...probe` used by torchrun exits 0 with no output for both help and bad input. All eight ranks would do nothing, then aggregation would report missing files.
2. **P0 — constructor deterministically raises after allocation:** payload passes `loop_reps=1` without `loop_range`; staged `from_hf_pretrained` requires `loop_range` whenever reps are positive. This checkpoint's actual training contract is range `16:32`, reps `1`.
3. **P0 — authorized GPUs are falsely rejected:** observed H100 nodes report `NVIDIA H100 80GB HBM3`, while the probe requires the literal substring `SXM`. Match the live authorized product-name variants plus 80 GB capacity/UUID uniqueness, not an absent marketing token.
4. **P0 — cache claim is invalid:** the custom block never passes cache to `RWKV7FeedForward`, so FFN token-shift state is omitted. A loop-enabled cache also aliases state because tied passes reuse each layer index. Test no-loop full model state (attention recurrent + attention token-shift + FFN token-shift) separately; give loop passes independent state identities or explicitly mark loop cache unsupported.
5. **P0 — aggregator can certify fiction:** eight `PASSED` records with only `checks=["runtime"]` and empty evidence pass; `rank-8.json` is ignored; `NaN` evidence passes. Require an exact allowed file set, ranks 0..7 exactly once, nonce/schema match, a fixed set of named real checks, required typed numeric fields, and recursive finite-number validation.
6. **P0 — reviewed bytes are not runtime-bound:** launcher references mutable repo/stage/model/checkpoint paths and mutable image tag `relay2:v2` but verifies no digest. Fail before CUDA/model allocation unless payload, staged model, config/index/HF shards, checkpoint, and metadata match `sourcehashes/sha256.txt`; emit those identities and job/image/package identities per rank.
7. **P1 — load the actual architecture safely:** actual HF geometry is 32 x 2,560, vocab 65,536, 40 heads; HF BF16 safetensors contain 2,947,735,040 elements; expected custom pre-loop model is 4,091,581,440 elements plus one loop gate. Attach loop `(16,32),1` before checkpoint load and assert exact state compatibility plus tensor **and numel** totals/category totals. The 16.37 GB checkpoint is pickle-based `model.pt`; keep `weights_only=True` at minimum, or stage safetensors before treating format safety as proven.
8. **P1 — failure producer is incomplete:** `AssertionError` from `assert_close` and API `TypeError` escape without a rank result. At this top-level process boundary, serialize every ordinary exception as a failed named check while preserving traceback/log detail; write atomically and fail if publication collides.
9. **P1 — distributed cleanup is incomplete:** retain local 1,500 s timeout + 30 s kill grace and torchrun `--max-restarts=0`, but set an explicit process-group timeout below the launcher bound and destroy the group in `finally` on every initialized rank.
10. **P1 — reject non-finite controls:** `--memory-fraction nan` currently returns READY/0. Require `math.isfinite` before range checks (and canonical 32-hex nonce rather than broad Unicode `isalnum`).
11. **P1 — make model proof substantive:** assert exact masked-logit shape `[1,48,65536]`, finite logits/cache delta/memory/timing, checkpoint step/hash, source hashes, versions, rank/local-rank/world, GPU inventory, and expected numel. Never mark an unavailable/skipped check as model proof.
12. **PASS receipts:** the existing six CPU tests pass (`6 passed in 0.44s`), shell syntax passes, missing/nonce/malformed/basic timeout/world-size controls fail closed, local timeout/restart bounds exist, and static scan found no training, optimizer, qz mutation, download, install, or git action.
13. **Residual uncertainty:** inspected local FLA 0.5.0 + Transformers 5.3.0 sources make `from fla.models.utils import Cache; Cache()` API-compatible, but the eventual mutable image tag is not proven to contain those bytes; runtime version/source checks remain mandatory.
14. **Authorization/resource status:** one 8x H100-SXM-80GB node, 30 minutes/4 GPU-hours, no auto-retries, spec `7166bd2e...` in LCG `71b971a7...` is expressible; **no job was submitted or stopped during this review**.
